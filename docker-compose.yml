services:
  bot:
    build: .
    container_name: wechat-bot
    stdin_open: true
    tty: true
    env_file: .env
    volumes:
      - wechat-bot-data:/app/data
    restart: always

volumes:
  wechat-bot-data:
